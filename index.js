


// un forma
// funtion de fleca
    const clificaDesafio = (a, b) => {
      
      // las vairbles se decakararon en una misma -----no estab segura si si era la formula adecuada 
      var[ a, b = 0]
         
      // cliclo for 
      for (i = 0; i < a.length; i++) {
          if ( a[i] > b[i] ) {
              a+=1 
          } else if ( a[i] < b[i]) {
              contB+=1
          } 
      };
  
     a[0] = a;
     b[1] = b;
  
      return result
     
  
  };
  console.log(clificaDesafio ['']);
  


